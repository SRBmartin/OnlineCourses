<Window x:Class="OnlineCourses.InformationSystem.Views.Dialogs.CourseDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        Width="380" Height="230"
        WindowStartupLocation="CenterOwner"
        WindowStyle="SingleBorderWindow"
        ResizeMode="NoResize"
        ShowInTaskbar="False">

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="76"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Name:"
               VerticalAlignment="Center" Padding="0,0,8,0"/>
        <TextBox Grid.Row="0" Grid.Column="1"
                 Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                 Padding="4,3" Margin="0,0,0,8"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Field:"
               VerticalAlignment="Center" Padding="0,0,8,0"/>
        <TextBox Grid.Row="1" Grid.Column="1"
                 Text="{Binding Field, UpdateSourceTrigger=PropertyChanged}"
                 Padding="4,3" Margin="0,0,0,8"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Lecturer:"
               VerticalAlignment="Center" Padding="0,0,8,0"/>
        <TextBox Grid.Row="2" Grid.Column="1"
                 Text="{Binding Lecturer, UpdateSourceTrigger=PropertyChanged}"
                 Padding="4,3" Margin="0,0,0,4"/>

        <TextBlock Grid.Row="3" Grid.ColumnSpan="2"
                   Text="{Binding ValidationError}"
                   Foreground="#C0392B" FontSize="11" Margin="0,2,0,0">
            <TextBlock.Style>
                <Style TargetType="TextBlock">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ValidationError}" Value="">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </TextBlock.Style>
        </TextBlock>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="76" Margin="0,0,8,0"
                    Command="{Binding CancelCommand}"/>
            <Button Content="Save" Width="76" IsDefault="True"
                    Command="{Binding SaveCommand}"/>
        </StackPanel>
    </Grid>
</Window>
